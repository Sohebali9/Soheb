package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Light Colors (Academic Premium Blue Theme)
val PrimaryBlue = Color(0xFF1E40AF) // Deep Royal Blue
val SecondaryBlue = Color(0xFF3B82F6) // Bright Azure Blue
val AccentGold = Color(0xFFD97706) // Rich Gold/Orange for streaks and score indicators
val LightBackground = Color(0xFFF8FAFC) // Very light slate gray
val LightSurface = Color(0xFFFFFFFF)

// Dark Colors (Midnight Exam Space)
val DarkBackground = Color(0xFF0F172A) // Intense Slate 900
val DarkSurface = Color(0xFF1E293B) // Slate 800 for card relief
val DarkPrimary = Color(0xFF60A5FA) // Accessible Azure Blue in dark mode
val DarkSecondary = Color(0xFF93C5FD) // Muted high-contrast sky blue

// Probability Accent Colors
val ProbHigh = Color(0xFF22C55E) // Confidence Green
val ProbMedium = Color(0xFFEAB308) // Warning Yellow
val ProbLow = Color(0xFFEF4444) // Error Orange-Red
